{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cyno.software/lmti/schemas/lmti.thoth.schema.json",
  "title": "LMTI Thoth JSON Boundary Envelope",
  "type": "object",
  "required": ["schemaVersion", "command", "status", "data", "warnings", "errors"],
  "additionalProperties": false,
  "properties": {
    "schemaVersion": { "const": "lmti.thoth.v1" },
    "command": { "type": "string", "minLength": 1 },
    "status": { "enum": ["pass", "warn", "blocked", "error"] },
    "data": { "type": "object" },
    "warnings": {
      "type": "array",
      "items": { "$ref": "#/$defs/message" }
    },
    "errors": {
      "type": "array",
      "items": { "$ref": "#/$defs/message" }
    }
  },
  "$defs": {
    "message": {
      "type": "object",
      "required": ["code", "message"],
      "additionalProperties": false,
      "properties": {
        "code": { "type": "string", "minLength": 1 },
        "message": { "type": "string", "minLength": 1 }
      }
    }
  }
}

